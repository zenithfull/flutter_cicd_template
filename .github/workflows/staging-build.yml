name: staging build

on:
  push:
    branches:
      - main

jobs:
  android-build:
    uses: ./.github/workflows/android-build.yml

  ios-build:
    needs: android-build
    uses: ./.github/workflows/ios-build.yml
